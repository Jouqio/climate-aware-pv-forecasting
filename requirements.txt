# ============================================================
# requirements.txt — climate-aware-pv-forecasting
# Berdasarkan import aktual dari NB01–NB10
# ============================================================

# ── Core data processing ─────────────────────────────────────
# NB01, 02, 03, 04, 05, 06, 07, 08, 09, 10 
pandas>=2.1.0
numpy>=1.26.0
pyarrow>=14.0.0        # wajib: pandas .to_parquet() / read_parquet()

# ── Scientific computing ─────────────────────────────────────
# NB02, 04, 05, 08, 09, 10 — scipy.stats (pearsonr, spearmanr, rankdata, dll)
scipy>=1.12.0

# ── Econometric & time series modeling ───────────────────────
# NB02: statsmodels.api (OLS)
# NB03: variance_inflation_factor
# NB05: het_white, het_breuschpagan, acorr_breusch_godfrey,
#       acorr_ljungbox, durbin_watson, jarque_bera, adfuller, kpss
# NB06: SARIMAX, plot_acf, plot_pacf, acorr_ljungbox
# NB09: acorr_ljungbox, adfuller
# NB10: statsmodels.api, plot_acf
statsmodels>=0.14.1

# ── Machine learning ─────────────────────────────────────────
# NB07: XGBRegressor, ParameterGrid, mean_squared_error
scikit-learn>=1.4.0
xgboost>=2.0.0

# ── Explainability / SHAP ────────────────────────────────────
# NB08: shap.TreeExplainer
shap>=0.45.0

# ── Visualization ────────────────────────────────────────────
# NB10: matplotlib, gridspec, patches, FancyArrowPatch, FancyBboxPatch
matplotlib>=3.8.0
